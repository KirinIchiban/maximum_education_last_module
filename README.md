## 📋 О проекте

Это веб-приложение на базе **Django**, представляющее собой площадку для размещения объявлений о продаже товаров.  

Пользователи могут просматривать объявления, а администраторы — управлять ими через удобную административную панель.

В репозитории лежит 2 версии проекта: 
- app_lesson_4 - создавалась на первом занятии, это цчебная версия для первой работы с фреймвокром django.
- app_advertisement - финальная версия, доработанная на занятиях

## 🛠 Функциональность

### Для посетителей:

- Просмотр всех объявлений на главной странице  
- Переход на страницу топ-продавцов  

### Для администраторов:

- Полноценная административная панель Django  
- Управление объявлениями (создание, редактирование, удаление)  
- Фильтрация объявлений по наличию торга  
- Поиск по заголовкам объявлений  
- Массовые операции с объявлениями  

## Технологии

- **Database:** SQLite (по умолчанию)  
- **Frontend:** HTML, CSS (шаблоны Django)  
- **Дополнительно:** Django Admin customization

### Установка и запуск

1. Клонируйте репозиторий
   ```bash
   git clone https://github.com/KirinIchiban/maximum_education_last_module
   cd maximum_education_last_module/advertisement
   
2. Создайте и активируйте виртуальное окружение
   ```bash
   # Windows
   python -m venv venv
   venv\Scripts\activate

   # Linux / Mac
   python3 -m venv venv
   source venv/bin/activate
   
3. Установите зависимости
   ```bash
   pip install django
   pip install pillow  # для работы с изображениями
   
4. Примените миграции
   ```bash
   python manage.py migrate
   
5. Создайте суперпользователя (для доступа в админку)
   ```bash
   python manage.py createsuperuser
   
6. Запустите сервер
   ```bash
   python manage.py runserver
   
## Модель данных
### Advertisement (Объявление)

| Поле        | Тип           | Описание                          |
|-------------|---------------|-----------------------------------|
| title       | CharField     | Заголовок объявления              |
| description | TextField     | Подробное описание товара         |
| price       | DecimalField  | Цена товара                       |
| auction     | BooleanField  | Возможность торга (Да/Нет)        |
| created_at  | DateTimeField | Дата и время создания              |
| update_at   | DateTimeField | Дата и время последнего обновления |
| user        | ForeignKey    | Ссылка на пользователя (автора)   |
| image       | ImageField    | Изображение товара                 |

## ⚙️ Особенности реализации
### Кастомизация админ-панели
  </> Python
  class AdvertisementAdmin(admin.ModelAdmin):
    list_display = ['id', 'title', 'price', 'auction', 'get_image']
    list_filter = ['auction', 'created_at']
    search_fields = ['title']
    actions = ['make_auction_as_false', 'make_auction_as_true']

Что реализовано:

- Отображение ID, заголовка, цены, наличия торга и изображения
- Фильтрация по наличию торга и дате создания
- Поиск по заголовкам
- Массовые действия:
- «Убрать возможность торга»
- «Добавить возможность торга»

### Интерактивные элементы в админке
  </> Python
  def created_date(self):
    if self.created_at.date() == timezone.now().date():
        return format_html(
            '<span style="color: green; font-weight: bold;">Сегодня в {}</span>', 
            created_time
        )  
 - Объявления, созданные сегодня, подсвечиваются зеленым
 - Объявления, обновленные сегодня, подсвечиваются коричневым
 - Миниатюры изображений отображаются прямо в списке объявлений
